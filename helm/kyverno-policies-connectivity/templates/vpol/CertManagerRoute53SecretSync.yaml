# THIS FILE IS GENERATED WITH 'make generate' - DO NOT EDIT MANUALLY
{{- if and .Values.certManager.syncSecret .Values.vpol.enabled }}
# create/update secret sync-cert-manager-route53-secret
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: "{{ .Release.Name }}-sync-cert-manager-route53-secret"
  annotations:
    "helm.sh/hook": "pre-install,pre-upgrade"
    "helm.sh/hook-weight": "-2"
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["create", "update", "get", "delete", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: "{{ .Release.Name }}-sync-cert-manager-route53-secret"
  annotations:
    "helm.sh/hook": "pre-install,pre-upgrade"
    "helm.sh/hook-weight": "-1"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: "{{ .Release.Name }}-sync-cert-manager-route53-secret"
subjects:
- kind: ServiceAccount
  name: kyverno-background-controller
  namespace: kyverno
---
apiVersion: policies.kyverno.io/v1beta1
kind: GeneratingPolicy
metadata:
  name: sync-cert-manager-route53-secret
  annotations:
    policies.kyverno.io/title: Sync cert-manager Route53 Secret
    policies.kyverno.io/category: Connectivity
    policies.kyverno.io/severity: Medium
    policies.kyverno.io/description: >-
      This policy synchronizes the cert-manager Route53 secret from the source namespace to the target namespace.
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: ['']
        apiVersions: ['v1']
        operations: ['CREATE']
        resources: ['namespaces']
  matchConditions:
    - name: match-namespace
      expression: >-
        object.metadata.name.startsWith("{{ trimSuffix "*" .Values.certManager.targetNamespace }}")
  evaluation:
    synchronize:
      enabled: true
  variables:
    - name: sourceSecret
      expression: resource.Get("v1", "secrets", "{{ .Values.certManager.sourceNamespace }}", "{{ .Values.cluster.name }}-cert-manager-user-secrets")
  generate:
    - expression: generator.Apply(object.metadata.name, [variables.sourceSecret])
{{- end }}
