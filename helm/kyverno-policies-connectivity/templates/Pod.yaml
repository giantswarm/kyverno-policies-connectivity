# THIS FILE IS GENERATED WITH 'make generate' - DO NOT EDIT MANUALLY
{{- if .Values.proxy.enabled }}
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: inject-proxy-env
spec:
  rules:
    - name: inject-proxy-env-to-containers
      match:
        resources:
          kinds:
            - Pod
{{- if .Values.proxy.namespaces }}
          namespaces:
          {{- range .Values.proxy.namespaces }}
            - {{ . | quote }}
          {{- end }}
{{- end }}
      mutate:
        patchStrategicMerge:
          spec:
            containers:
              - (name): "*"
                env:
                  {{- if .Values.proxy.secret.http }}
                  - name: HTTP_PROXY
                    value: {{ .Values.proxy.secret.http }}
                  - name: http_proxy
                    value: {{ .Values.proxy.secret.http }}
                  {{- end }}
                  {{- if .Values.proxy.secret.https }}
                  - name: HTTPS_PROXY
                    value: {{ .Values.proxy.secret.https }}
                  - name: https_proxy
                    value: {{ .Values.proxy.secret.https }}
                  {{- end }}
                  {{- if .Values.proxy.noProxy }}
                  - name: NO_PROXY
                    value: {{ join "," .Values.proxy.noProxy }}
                  - name: no_proxy
                    value: {{ join "," .Values.proxy.noProxy }}
                  {{- end }}
            =(initContainers):
              - (name): "*"
                env:
                  {{- if .Values.proxy.secret.http }}
                  - name: HTTP_PROXY
                    value: {{ .Values.proxy.secret.http }}
                  - name: http_proxy
                    value: {{ .Values.proxy.secret.http }}
                  {{- end }}
                  {{- if .Values.proxy.secret.https }}
                  - name: HTTPS_PROXY
                    value: {{ .Values.proxy.secret.https }}
                  - name: https_proxy
                    value: {{ .Values.proxy.secret.https }}
                  {{- end }}
                  {{- if .Values.proxy.noProxy }}
                  - name: NO_PROXY
                    value: {{ join "," .Values.proxy.noProxy }}
                  - name: no_proxy
                    value: {{ join "," .Values.proxy.noProxy }}
                  {{- end }}
            =(ephemeralContainers):
              - (name): "*"
                env:
                  {{- if .Values.proxy.secret.HTTP }}
                  - name: HTTP_PROXY
                    value: {{ .Values.proxy.secret.http }}
                  - name: http_proxy
                    value: {{ .Values.proxy.secret.http }}
                  {{- end }}
                  {{- if .Values.proxy.secret.https }}
                  - name: HTTPS_PROXY
                    value: {{ .Values.proxy.secret.https }}
                  - name: https_proxy
                    value: {{ .Values.proxy.secret.https }}
                  {{- end }}
                  {{- if .Values.proxy.noProxy }}
                  - name: NO_PROXY
                    value: {{ join "," .Values.proxy.noProxy }}
                  - name: no_proxy
                    value: {{ join "," .Values.proxy.noProxy }}
                  {{- end }}
{{- end }}
