proxy:
  enabled: true

cluster:
  name: "test-cluster"
  proxy:
    http: "http://proxy.example.com:8080"
    https: "http://proxy.example.com:8080"
    noProxy: "some.no-proxy.domain,10.0.0.0/8"

wcIpCheck:
  enabled: false
  deriveFromControlPlaneEndpoint:
    enabled: true
